apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp-deployment
  namespace: backend-ns
  labels:
    app: webapp
spec:
  replicas: 1
  selector: 
    matchLabels:
      app: webapp
  template:
    metadata:
      labels:
        app: webapp
    spec:
      containers:
      - name: webapp
        image: demonagp.azurecr.io/weatherforecast:021d4ff5d9f13b933ac73437ba619a4215fd059f
        ports: 
        - containerPort: 80
        env:
        - name: MSSQL_SERVER
          valueFrom:
            configMapKeyRef:
              name: mssql-config
              key: mssql-server
        - name: MSSQL_PORT
          valueFrom:
            configMapKeyRef:
              name: mssql-config
              key: mssql-port
        - name: MSSQL_PASS
          valueFrom:
            secretKeyRef:
              name: mssql-secret-backend
              key: mssql-pass

---
apiVersion: v1
kind: Service
metadata:
  name: webapp-service
  namespace: backend-ns
spec:
  type: LoadBalancer
  selector:
    app: webapp
  ports:
  - name: webapp-port
    protocol: TCP
    port: 8080
    targetPort: 80
